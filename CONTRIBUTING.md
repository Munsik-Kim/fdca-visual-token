# Contributing

Use a focused branch and pull request. Run `make verify test reproduce` before submission. Never submit checkpoints, datasets, private paths, secrets, or generated caches. Scientific-number changes require a preregistered analysis and evidence mapping.
