# Privacy and publication scope

This repository was built in a new directory without private `.git` history. It excludes private absolute paths, tokens, datasets, full run directories, model and metric weights, external clones, environments, caches and logs. Source-to-public hashes and private-path inventories remain in a separate non-public publication log.
